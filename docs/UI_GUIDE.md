# UI 디자인 가이드

갱신일: 2026-09-20
상태: 제안. 화면 스택은 [ADR-0002](adr/0002-server-stack-python-fastapi-sqlite.md)의 Jinja2 서버 렌더링 + CSS + 소량 JavaScript다. Tailwind·웹폰트·외부 CDN·아이콘 라이브러리를 쓰지 않는다. 상태명·문구는 [GLOSSARY](GLOSSARY.md), 상태와 이유의 대응은 [PRD 3절](PRD.md)을 그대로 쓴다. 경로는 제안이며 구현 step에서 확정한다.

## 참고 화면과 대응

사용자가 2026-09-20 공유한 Devin 세션 화면(왼쪽 세션 목록, 가운데 작업 타임라인과 PR 카드, 오른쪽 보고서 뷰어)의 구성과 밀도를 따른다. 그 화면은 에이전트와 대화하는 UI지만 이 제품에는 대화가 없다. 아래 대응만 가져오고 대화 입력은 만들지 않는다.

| 참고 화면 요소 | 이 제품의 대응 |
|---|---|
| 왼쪽 Recent 세션 목록 | 내 세션의 업무 목록. 제목, 상대 시각, 상태 배지 |
| 가운데 상단 브레드크럼 `Website / Migrate…` | `{선택된 에이전트 이름} / {업무 제목}`. 미선택이면 `에이전트 미선택` |
| 사용자 요청 말풍선 | `Task.request` 회색 말풍선과 등록 시각 |
| 접이식 행 `Used playbook: Test` | 접이식 행 `자동 선택 · agent-ops-demo · 후보 1개`, `완료 기준 · 자동 완료` |
| 접이식 행 `Worked for 4m 13s +25 -131` | 실행 블록 헤더 `실행 1회차 · 4분 13초 · +21 -8`. 펼치면 이벤트 타임라인 |
| PR 카드 `Open · repo #167 · branch → main · 6 files` | 결과 카드. outcome 배지, 요약, 저장소·커밋·파일 수·증감 또는 근거 수·검증 통과 수 |
| 첨부 파일 칩 `test_gradient_migration.md` | 산출물 칩 `diff`, `테스트 전`, `테스트 후`, `보고서`, `진단 결과`. 누르면 오른쪽 열에 연다 |
| `Devin is ready for instructions` 상태 줄 | 상태 배지 + 이유 문구 한 줄 |
| 하단 채팅 입력창 | 동작 영역. 실행 버튼, 또는 검토 의견 입력과 `완료 승인`·`수정 요청`·`종료` |
| 오른쪽 보고서 뷰어, Before/After 비교 | 산출물 뷰어. `수정 전 / 수정 후`, `변경 전 응답 / 변경 후 응답` 두 칸 비교 |
| 우상단 `#357 + 1` 연결 PR 버튼 | `선행 A` · `후속 B` 연결 업무 칩 |

## 디자인 원칙

1. 도구처럼 보인다. 홈 상단에 브랜드(로고 `/static/logo.jpg`, 한 줄 설명, 흐름 한 줄)만 두고 그 아래가 곧 에이전트·업무 목록이다. 별도 랜딩 페이지는 없다.
2. 상태와 이유는 항상 한 쌍이다. 배지 옆에 PRD 3절의 이유 문구를 붙이고, 접수와 시작을 시각적으로 구분한다.
3. 근거가 결론보다 먼저 보인다. 결과 카드와 뷰어는 검증 결과·근거 원문·산출물을 위에, 에이전트의 요약 문장을 아래에 둔다.
4. 심사자는 처음 왔고 2분 있다. 모든 화면에 다음 행동 버튼이 하나 있고, 빈 목록에는 빈 상태 문장과 그 버튼이 있다.

## 하지 마라

| 금지 사항 | 이유 |
|-----------|------|
| `backdrop-filter: blur()`, 배경 그라데이션 원, 글로우, 그라데이션 텍스트 | AI 템플릿 징후 |
| 보라·인디고 계열 강조색 (로고 이미지 안의 색은 예외, UI 색으로 끌어오지 않는다) | "AI = 보라색" 클리셰 |
| "Powered by AI" 류 배지 | 기능이 아니라 장식 |
| 에이전트와 자유 대화하는 입력창 | 이 제품에 대화가 없다. 하단 영역은 실행·검토 동작만 |
| 상태를 색으로만 표시 | 흑백 스크린샷·색약에서 구분 불가. 항상 한글 상태 텍스트 병기 |
| `pending`, `done`, `success`, `error`, `대기 중` 등 GLOSSARY 금지 표현 | 코드 식별자와 화면 문구가 갈라진다 |
| 스피너로 `실행 요청됨`과 `실행 중`을 뭉개기 | 접수와 시작 구분이 제품 원칙이다 |
| 원문 JSON을 기본 노출 | "원문 보기" 토글 뒤에 둔다 |
| `alert()`·`confirm()` | 검토 동작은 인라인 폼 |

## 앱 셸 — 3열

| 열 | 너비 | 내용 |
|---|---|---|
| 왼쪽 목록 | 260px 고정, 배경 `--bg-side` | 상단 제품명. 탐색 `업무`, `에이전트`, 운영자 쿠키가 있을 때만 `운영자`. `최근` 헤더와 `+`(시연 업무 만들기). 내 업무 목록. 하단 `세션` 정보 |
| 가운데 | 나머지, 최대 760px 본문 | 브레드크럼 헤더. 아래는 화면별 본문 |
| 오른쪽 뷰어 | 420px, 접을 수 있음 | 산출물 뷰어. 열 만한 산출물이 없으면 숨김 |

- 1200px 미만: 오른쪽 뷰어는 산출물 칩을 눌렀을 때 슬라이드 오버로 열린다.
- 800px 미만: 한 열. 왼쪽 목록은 상단 바의 `목록` 버튼으로 여는 서랍, 산출물은 전체 화면 페이지. 온라인 투표 방문자는 휴대폰일 수 있으므로 390px에서 가로 스크롤이 없어야 한다.
- 왼쪽 목록 항목: 제목 14px, 아래 줄 12px `--text-muted`로 `12분 전 · 실행 중` 형식. 상태 점은 아래 상태 표시 절과 같다. 읽지 않음 표시는 첫 범위에서 제외한다.

## 화면 목록

모든 화면이 위 셸 안에 있다.

| 화면 | 경로(제안) | 대상 | 가운데 열 |
|---|---|---|---|
| 홈 | `/` | 세션 | 브랜드(로고·한 줄 설명·흐름), 에이전트 연결 상태 카드(→ `/agents`), 업무: 버튼 `시연 업무 만들기`·`직접 등록`, 목록 또는 빈 상태 문장 "아직 업무가 없습니다." |
| 업무 등록 | `/tasks/new` | 세션 | PRD 2절 입력 항목. `?example=diagnose`, `?example=fix&predecessor={task_id}`로 미리 채움 |
| 업무 상세 | `/tasks/{task_id}` | 세션 | 아래 "업무 상세" 절 |
| 산출물 | `/tasks/{task_id}/artifacts/{artifact_id}` | 세션 | 오른쪽 뷰어와 같은 내용을 단독 페이지로. 800px 미만과 직접 링크용 |
| 에이전트 목록 | `/agents` | 세션(읽기 전용) | 이름, 소유 구분, 능력, 연결 상태, 마지막 확인 시각 |
| 에이전트 상세 | `/agents/{agent_id}` | 세션(읽기 전용) | 능력 목록, 파악된 정보와 확인 수준("설정 발견" / "실제 사용 확인"), 최근 실행 |
| 운영자 | `/operator` | 운영자 | 토큰 입력. 에이전트 등록·수정·삭제, 연결 코드 발급·취소, 병합 확인 대기 목록, 모든 세션 업무, 진단 예산 사용량 |

운영자 등록 폼은 "자동 파악된 값"과 "소유자가 고친 값"을 나란히 보이고, 읽어오지 못한 항목은 빈칸과 "직접 입력"으로 표시한다.

## 심사자 첫 방문 흐름

1. 접속. 세션 쿠키가 발급되고 홈이 열린다. 왼쪽 목록은 비어 있고, 가운데 상단에 로고와 한 줄 설명, 그 아래 에이전트 2개 연결 상태 카드와 버튼 `시연 업무 만들기`·`직접 등록`이 있다.
2. 버튼을 누르면 업무 등록 폼이 진단 업무 A로 미리 채워져 열린다. 자동 선택, 직접 실행, 자동 완료, 완료 기준 템플릿이 채워져 있고 고칠 수 있다. 등록 한 번으로 저장한다.
3. A 상세. 상태 줄 `실행 가능 · agent-ops-demo 선택됨`, 동작 영역에 `실행`. 브레드크럼 우측 안내 `후속 업무 B 등록`이 링크로 있다. 시연 폼(`?example=diagnose`)은 `후속 업무 B(코드 수정)도 함께 등록` 체크박스가 기본 체크라 보통 B 는 이미 `대기 · 선행 대기` 로 있다.
4. B 등록 폼. 선행 업무 A, 자동 선택, 자동 실행, 검토 후 완료가 채워져 있다. 등록하면 B 상세는 `대기 · 선행 대기`를 보이고 왼쪽 목록에 A와 B가 함께 보인다.
5. A 실행. `실행 요청됨`에서 `실행 중`으로 바뀌고 실행 블록의 진행 메시지가 갱신된다. 끝나면 결과 카드 `인계 가능`과 `완료 · 판정 근거`, 또는 `확인 필요 · 미충족 항목`이 보인다. 오른쪽 뷰어에 `진단 결과`가 자동으로 열린다.
6. B. A 완료 뒤 자동 착수하면 A 화면의 후속 칩이 `실행 중`으로 바뀐다. 연결 프로그램이 오프라인이면 `대기 · 연결 끊김, 마지막 확인 {시각}`이다. 시작했다고 표시하지 않는다.
7. B 결과. 결과 카드 `검토 가능`, 상태 줄 `확인 필요 · 검토 대기`, 뷰어에 `검증 요약`(수정 전/후 테스트, 보고서). 동작 영역에서 `완료 승인`을 누르면 `완료`와 "병합: 운영자 확인 대기"가 남는다.

시연 예시 A의 완료 방식만 기본값과 다르게 자동 완료로 채운다. 자동 완료가 가능한 유일한 업무 종류이고, 그래야 사람 조작 없는 인계가 화면에 보인다. 심사자는 폼에서 검토 후 완료로 바꿀 수 있다.

## 업무 상세 — 가운데 열의 순서

1. 브레드크럼 `agent-ops-demo / 일일 보고서 실패 진단`. 우측에 연결 업무 칩 `후속 B`와 뷰어 접기 버튼.
2. 요청 말풍선. `Task.request` 전문, 등록 시각. 배경 `--bg-bubble`, 반경 12px.
3. 접이식 행 `자동 선택 · agent-ops-demo · operations.diagnose 일치 후보 1개`. 펼치면 CONTRACT 9절의 선택 기록 표. `reason` 문자열은 그대로 출력한다.
4. 접이식 행 `완료 기준 · 자동 완료 · 결과가 ready_for_handoff이고 근거 검증 통과`. 펼치면 기준 항목 목록.
5. 실행 블록, 시도마다 하나. 헤더 `실행 1회차 · 4분 13초 · +21 -8`(코드 수정) 또는 `실행 1회차 · 1분 40초 · 도구 호출 6회`(진단). 실행 중에는 경과 시간이 갱신된다. 펼치면 `seq`, 시각, `type`, 메시지의 이벤트 타임라인. 접혀 있어도 마지막 `progress` 메시지 한 줄은 본문 텍스트로 보인다.
6. 결과 카드. 아래 "결과 카드" 절.
7. 산출물 칩 줄. 결과 카드 아래에 가로로 나열. 누르면 오른쪽 뷰어에 연다.
8. 상태 줄. 상태 배지 + 이유. 예 `실행 중 · 재현 테스트 작성, 수정 전 실행 실패 확인`, `대기 · 연결 끊김, 마지막 확인 10:12:03`, `확인 필요 · 검토 대기`.
9. 동작 영역. 상태에 따라 하나만 보인다.
   - `실행 가능`: 버튼 `실행`, 링크 `에이전트 직접 선택`.
   - `확인 필요`(검토 대기 또는 판정 불가): textarea `검토 의견`과 버튼 `완료 승인`, `수정 요청`, `종료`. 승인 뒤에는 "병합: 운영자 확인 대기"를 같은 자리에 남긴다.
   - `확인 필요`(후보 0개 또는 2개 이상): 에이전트 선택 목록과 버튼 `확정`.
   - 그 외: 비어 있다.

## 결과 카드

PR 카드 형식. 테두리 카드, 첫 줄에 outcome 배지와 대상, 둘째 줄에 요약 한 줄, 셋째 줄에 모노스페이스 메타.

| 종류 | 첫 줄 | 셋째 줄 메타 |
|---|---|---|
| 진단 | 배지 + `daily-report · daily-0920-0900` | `response_path_changed` · `근거 5 · 첨부 8 · 검증 6/6 통과` |
| 코드 수정 | 배지 + `demo-report-repo · fix-daily-0920` | `9b7e4d2` ← `3f9c2e1` · `3 files` · `+21 -8` · `vp-pytest exit 0` |

outcome 표시 라벨. 코드 값은 GLOSSARY, 라벨은 여기서만 정한다.

| outcome | 라벨 | 배지 색 |
|---|---|---|
| `ready_for_handoff` | `인계 가능` | `--state-done` |
| `ready_for_review` | `검토 가능` | `--state-attention` |
| `needs_information` | `정보 필요` | `--state-attention` |

증감 수는 `+21` `--state-done`, `-8` `--state-failed` 모노스페이스. 카드 우측에 `뷰어에서 열기` 아이콘 버튼.

## 오른쪽 열 — 산출물 뷰어

- 헤더: 산출물 이름, `kind` 배지, 크기, 버튼 `원문 보기`, `복사`, `다운로드`.
- 기본 내용. 진단이면 `진단 결과`, 코드 수정이면 `검증 요약`. 칩을 누르면 개별 산출물로 바뀐다.
- `진단 결과` 렌더링 순서: 검증 결과(통과·미충족 항목) → `summary` → `findings`(claim과 근거 칩) → `diagnosis` 표 → `repair_request` → `missing_information`. `needs_information`이면 `missing_information`을 맨 위로. 두 칸 비교 `변경 전 응답 / 변경 후 응답`을 `findings` 아래에 두고 `old_path`·`new_path` 값을 강조한다.
- `검증 요약` 렌더링 순서: `verification`(프로필, 종료 코드, 결과 커밋) → 두 칸 비교 `수정 전 테스트 / 수정 후 테스트`(각 로그 마지막 20줄) → 생성된 `보고서` → `summary`.
- 근거 칩 `evidence_id@version · location`은 모노스페이스. 누르면 바로 아래에 첨부 원문의 해당 값 또는 줄 범위를 펼친다. 원문은 서버가 첨부에서 잘라 준다. 모델 문장을 원문 대신 넣지 않는다.
- `diff`는 `<pre>`에 추가 줄 `--bg-done`, 삭제 줄 `--bg-failed`. 로그는 `<pre>` 모노스페이스 13px, 줄 번호 표시.
- 두 칸 비교는 참고 화면의 Before/After 카드와 같다. 각 칸 위에 12px 라벨, 안에 테두리 상자, 아래에 한 줄 설명.

## 색상

```css
:root {
  --bg-page: #ffffff;      --bg-side: #fafafa;      --bg-card: #ffffff;     --bg-bubble: #f3f4f6;
  --border: #e5e7eb;       --border-strong: #d1d5db;
  --text: #111827;         --text-body: #374151;    --text-muted: #6b7280;  --text-disabled: #9ca3af;
  --accent: #1d4ed8;       /* 링크, 포커스 링, 진행 중 */
  --state-wait: #6b7280;   --state-ready: #111827;  --state-inflight: #1d4ed8;
  --state-attention: #b45309; --state-done: #047857; --state-failed: #b91c1c;
  --bg-wait: #f3f4f6;      --bg-inflight: #eff6ff;  --bg-attention: #fffbeb;
  --bg-done: #ecfdf5;      --bg-failed: #fef2f2;
  --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  --sans: -apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", "Malgun Gothic", "Noto Sans KR", system-ui, sans-serif;
}
```

라이트 테마 하나만 둔다. 컴포넌트는 변수만 참조한다.

## 상태 표시

배지는 연한 배경 알약 + 점 + 한글 상태 텍스트다. 점의 채움은 의미를 갖는다. 빈 점은 상대 쪽에서 아직 확인된 사실이 없다는 뜻, 채운 점은 확인된 사실이다.

| 사용자 상태 | 점 | 글자·점 색 | 배경 | 이유 문구 예 |
|---|---|---|---|---|
| `대기` | 빈 점 | `--state-wait` | `--bg-wait` | 선행 대기 / 연결 끊김, 마지막 확인 10:12:03 |
| `실행 가능` | 빈 점 | `--state-ready` | `--bg-wait` | agent-ops-demo 선택됨 |
| `실행 요청됨` | 빈 점 | `--state-inflight` | `--bg-inflight` | 접수 확인 / 접수 대기 |
| `실행 중` | 채운 점, 맥동 | `--state-inflight` | `--bg-inflight` | 마지막 진행 메시지 |
| `확인 필요` | 채운 점 | `--state-attention` | `--bg-attention` | 검토 대기 / 후보 2개, 선택 필요 / 시작 여부 불명, 재실행하지 않음 |
| `완료` | 채운 점 | `--state-done` | `--bg-done` | 판정 근거 / 검토 승인 |
| `실패` | 채운 점 | `--state-failed` | `--bg-failed` | 실패 코드·메시지 |

내부 Execution 상태(`queued`, `accepted`, `running`…)는 실행 블록 안에서만 보조 텍스트로 보인다. 에이전트 연결 상태는 `연결됨`(채운 점, `--state-done`)과 `연결 끊김`(빈 점, `--state-wait`) 둘이고 항상 "마지막 확인 {시각}"을 붙인다.

## 컴포넌트

- 카드: `background: var(--bg-card); border: 1px solid var(--border); border-radius: 10px; padding: 16px 20px`. 그림자 없음.
- 버튼: 기본 `background: var(--text); color: #fff`, 보조 `background: #fff; border: 1px solid var(--border-strong)`, 종료·거절 `color: var(--state-failed); border: 1px solid currentColor`. 높이 36px, 가로 여백 14px, 반경 8px. 비활성은 사유를 옆 문구로 표시한다.
- 입력: `border: 1px solid var(--border-strong); border-radius: 8px; padding: 9px 12px`. 포커스 `outline: 2px solid var(--accent); outline-offset: 1px`. 라벨 위, 도움말 아래 12px.
- 배지·칩: 높이 24px, 반경 6px, 12px/500. 산출물 칩은 테두리 + 파일 아이콘 + 이름.
- 접이식 행: 왼쪽 chevron, 13px `--text-muted`, 펼치면 아래에 표. 상태는 `<details>`로 구현해도 된다.
- 오류 알림: 카드 상단 인라인, `--bg-failed`. API 오류 본문의 `message`를 그대로, `code`는 모노스페이스 12px. 429는 `details.resets_at`을 "다시 가능: {시각}"으로 붙인다.
- 빈 상태: 한 문장 + 버튼 하나.

## 타이포그래피

| 용도 | 스타일 |
|---|---|
| 브레드크럼·카드 제목 | 15px / 600 / `--text` |
| 뷰어 문서 제목 | 20px / 600 |
| 본문·요청 말풍선 | 14px / 400 / `--text-body` / 행간 1.6 |
| 목록 보조·시각·이유·접이식 행 | 12~13px / `--text-muted` |
| ID, 커밋, 경로, 능력 코드, 로그, diff, 증감 수 | `--mono` 13px |

시각은 `2026-09-20 10:12:03 KST` 절대 시각으로, 목록과 "마지막 확인"만 `12분 전`을 쓴다. 서버는 UTC 저장, 템플릿에서 Asia/Seoul 변환.

## 실시간 갱신

- 상태 줄, 실행 블록, 결과 카드, 산출물 칩, 동작 영역, 왼쪽 목록의 상태를 `data-live` 조각으로 두고 3초마다 교체한다. 입력 중인 textarea가 있는 조각은 교체하지 않는다.
- 상태 줄 옆에 "마지막 갱신 10:12:03". 실패하면 "갱신 실패, 재시도 중"으로 바꾸고 화면을 멈추지 않는다.
- 업무가 `완료`·`실패`이면 10초로 늦춘다. 후속 업무 칩의 상태도 같은 조각에 넣어 페이지를 옮기지 않아도 B 착수가 보인다.

## 애니메이션과 아이콘

- 허용: 조각 교체 시 opacity 0.2s, `실행 중` 점 1.2s 맥동, 슬라이드 오버 0.2s. `prefers-reduced-motion`이면 맥동을 끈다. 그 외 금지.
- 아이콘은 인라인 SVG 소수만: chevron, 파일, 복사, 열기, 다운로드, 검색, 목록. `stroke-width: 1.5`, 16px, 둥근 배경 상자로 감싸지 않는다.
